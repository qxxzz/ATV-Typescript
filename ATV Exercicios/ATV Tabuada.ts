/**
 * Exibe a tabuada de 1 a 10 de um número fornecido.
 */
let base: number = 7; // número para gerar a tabuada (alterar para testar)

// Loop de 1 a 10 para calcular e exibir a tabuada
for (let i = 1; i <= 10; i++) {
  console.log(`${base} x ${i} = ${base * i}`); // mostra o resultado da multiplicação
}
