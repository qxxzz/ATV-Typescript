/**
 * Verifica se uma pessoa é maior ou menor de idade (>=18).
 */
var idadePessoa = 22; // idade a ser verificada (alterar para testar)

// Verifica se a idade é maior ou igual a 18
if (idadePessoa >= 18) {
    console.log("Maior de idade."); // maior de idade
} else {
    console.log("Menor de idade."); // menor de idade
}
